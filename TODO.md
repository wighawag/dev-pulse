# TODO

- **GitHub App for `@whitesmith` autocomplete**: Register a GitHub App named "whitesmith" so that `@whitesmith` becomes a mentionable bot user with autocomplete in GitHub comments. Currently using `/whitesmith` slash command as trigger, which works but lacks autocomplete. A GitHub App would enable the `@whitesmith` mention UX similar to `@claude`.
